{
  "environments": {
    "test": {
      "env": {
      },
      "buildpacks": [
        {
          "url": "https://github.com/heroku/heroku-buildpack-ci-postgresql"
        }
      ],
      "scripts": {
        "test-setup": "cd server; ./setup.sh $DATABASE_URL",
        "test": "cd server; ./sanity.sh $DATABASE_URL"
      }
    }
  }
}
